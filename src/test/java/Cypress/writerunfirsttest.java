package Cypress;

public class writerunfirsttest {
	
	/*-----------------How to write and run your first test
	 * we can write the first test case(specification) in cypress application (or) in vscode
	 * The work  that u do in cpypress application will reflect in vscode and in the same in vscode also reflected in cypress application
	 * We have to  create specs (test cases) in e2e(End to End ) folder in vs code  
	 * 
	 * 
	 * 
	 * The spec should contain describe block where we mention (suite  name followed by arrow function
	 * and alos it contain it arrow function .like that one  spec file can contain multiple describe blocks and multiple it blocks
	 * we can write arrow function with arrow keyword (or) function keyword 
	 * 
	 * cy is the root module like (root package ) where all the methods are present by which we can do the all the necessary acation on it
	 * 
	 * 
	 * we can execute the sepcs in file by following 2 approaches 1. by using cypress applciation by clicking on file
	 * 2.by using the command npx cypress run ----it will execute the fie in e2e
	 * for everytime to launch the cypress application we need to use the command called npx cypress open
	 * 
	 * npx cypress open ---this command will launch the cypress application
	 * npx cypress run ---it will run all the specs in headless mode
	 * npx cypress run --spec (path of the special file )--headed ----execution of specifi file in headed mode
	 * npx cypress run --browse chrome ---sepc this will execute the specific file in chrome browser with headed mode.
	 * 
	 * 
	 * npm install -D cypress-xpath  -----------we need to add this command in terminal to add cypress xpath plugin,so that
	 * we can locate the elements by using xpath also.
	 * 
	 *
	 *  
	 *  
	 *Test suit------------Test cases----------Test steps
	 *myfirsttest.cy.js----describe block---------it block  
	 * 
	 * 
	 */

}
